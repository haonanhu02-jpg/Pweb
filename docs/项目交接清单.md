# 项目交接清单

生成日期：2026-07-07  
交接人：徐小龙

| 序号 | 项目名称 | 客户 | 当前进度 | 存在的问题/注意点 | 交接人 | 主要附件文档清单 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 国央企招标公告采集 POC | 英科弹簧/内部业务验证 | 已完成 Python 采集框架、标准公告 JSON、SQLite 入库去重、JSONL 导出；已接入 `ggzy`、`sdic`、`cec`、`crrc`、`chnenergy` 5 个公开平台。 | 目前只采公开页面，不处理登录、验证码、CA、短信验证和供应商账号权限；真实高价值订单可能在央企自有平台登录区。 | 徐小龙 | [离职交接文档](./离职交接文档.md)<br>[README](../README.md) |
| 2 | 弹簧采购需求识别规则 | 英科弹簧/内部业务验证 | 已实现规则引擎，能按 `high`、`review`、`low`、`excluded` 分级；当前 353 条公告中识别到 1 条 `low` 线索。 | 当前公开样本没有稳定产出 `high/review`；需要继续接入更多高价值平台，并人工收集真实弹簧采购正样本校准规则。 | 徐小龙 | `src/tender_poc/screening/spring.py`<br>`tests/test_spring_screening.py` |
| 3 | 附件下载与解析 | 英科弹簧/内部业务验证 | 已支持 PDF、DOCX、XLSX、XLS、ZIP 的文本解析；当前附件解析统计为 `parsed=18`、`failed=3`。 | 不做 OCR，扫描版 PDF 可能解析为空；`.doc` 在本机缺少 `antiword` 时会记录失败但不阻断采集；RAR 暂不处理。 | 徐小龙 | `src/tender_poc/attachments.py`<br>`tests/test_attachment_parsing.py` |
| 4 | 招标商机可视化后台 | 英科弹簧/内部业务验证 | 已完成 FastAPI 后端和 React/Vite 前端；支持看板、商机列表、详情证据、附件预览和人工复核状态。 | 当前不做登录鉴权，只适合本机或内网验证；正式部署前需要补认证、权限、审计和外网安全配置。 | 徐小龙 | `src/tender_poc/web_api.py`<br>`web/src/App.tsx`<br>`web/src/api.ts` |
| 5 | Crawlab 与 Docker 本机部署 | 英科弹簧/内部业务验证 | 已提供 Docker Compose，包含 `mongo`、`crawlab`、`api`、`web` 4 个服务；Crawlab 负责调度现有 CLI，不承载业务逻辑。 | Docker Desktop 必须先启动；SQLite 仍是 POC 存储，多个 Spider 要错峰调度，避免并发写库；`data/` 和 `docker-data/` 不在 GitHub 中，需要单独迁移。 | 徐小龙 | [Crawlab 本地部署说明](../CRAWLAB_LOCAL.md)<br>`docker-compose.crawlab.yml` |
| 6 | GitHub 仓库与交接资料 | 英科弹簧/内部业务验证 | 代码已提交到 GitHub 仓库 `https://github.com/haonanhu02-jpg/Pweb`，主分支为 `main`；交接文档已放入 `docs/`。 | 仓库只包含源码、配置、测试和文档；不包含本机数据库、附件、原始 HTML、Docker 持久化数据和生成物。接收人如需复现当前 353 条数据，需要单独迁移 `data/` 目录。 | 徐小龙 | 本文件<br>[离职交接文档](./离职交接文档.md)<br>`.gitignore` |

## 接收人优先检查项

- [ ] 能访问 GitHub 仓库 `https://github.com/haonanhu02-jpg/Pweb`。
- [ ] 能拉取 `main` 分支代码。
- [ ] 能打开 [离职交接文档](./离职交接文档.md)。
- [ ] 能启动 Docker Desktop。
- [ ] 能执行 `docker compose -f docker-compose.crawlab.yml up -d --build`。
- [ ] 能打开前端 `http://localhost:5173`。
- [ ] 能打开 Crawlab `http://localhost:8080`。
- [ ] 知道 `data/` 目录是本机运行数据，不在 GitHub 仓库中。
